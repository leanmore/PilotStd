# PilotStd 前端环境变量模板
# 复制为 .env.local（或 .env）后按需修改。.env.local 不会被 git 追踪。

# 超级用户用户名（前端用于判断当前登录用户是否为超管）
VITE_SUPERUSER_USERNAME=superadmin
# 公告记录分页化开关（Phase 2）：
#   true  → 详情页/标准状态页走分页端点（每次滚动请求一页）
#   false → 全量数据前端分片（默认，向后兼容）
VITE_USE_PAGINATED_RECORDS_API=false
