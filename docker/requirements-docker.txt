# ==================================
# PilotStd Docker 环境依赖（无 GUI/桌面组件）
# 共享依赖通过 -r ../requirements.txt 引用
# ==================================

-r ../requirements.txt

# --- Docker Web 服务 ---
fastapi==0.136.0
pydantic>=2.0.0
uvicorn[standard]
python-multipart>=0.0.31

# --- 定时任务与 JWT ---
apscheduler==3.10.4  # 回退：4.x 在 PyPI 上被 yanked（2026-06-27），待 4.0.2+ 稳定后重新评估
python-jose[cryptography]==3.5.0
starlette>=1.3.1
pyasn1>=0.6.3

# --- 测试依赖（CI 环境） ---
responses>=0.25.0

# --- 系统监控 ---
psutil==7.2.2
