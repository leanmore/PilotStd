# ==================================
# PilotStd Docker 环境依赖（无 GUI/桌面组件）
# 与 ../requirements.txt 同步维护，去掉 PyQt6 等桌面库
# ==================================

# --- 核心网络请求与网页解析 ---
requests==2.34.0
httpx==0.28.0
beautifulsoup4==4.14.0
lxml==6.1.0

# --- 数据处理与 Excel 导出 ---
pandas==3.0.0
openpyxl==3.1.0

# --- 验证码识别与图像处理（下载模块需 OCR） ---
ddddocr==1.6.1
pillow==12.2.0

# --- 文档解析（公告附件 PDF/DOCX） ---
PyPDF2==3.0.1
python-docx==1.1.2

# --- Docker Web 服务 ---
fastapi==0.136.0
pydantic>=2.0.0
uvicorn==0.46.0
python-multipart==0.0.27

# --- 定时任务与 JWT ---
apscheduler==3.10.4  # 回退：4.x 在 PyPI 上被 yanked（2026-06-27），待 4.0.2+ 稳定后重新评估
python-jose[cryptography]==3.5.0
starlette>=1.0.1
pyasn1>=0.6.3

python-dotenv==1.1.0

# --- 文件系统监控（扫描模块）---
watchdog==6.0.0

# --- WinUI 通知中心 ---
websocket-client==1.9.0
