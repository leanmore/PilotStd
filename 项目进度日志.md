# 项目进度日志

## 2026-07-04

### WinUI 公告互斥逻辑修复
- 根因：`_config_load.py` 中 `setChecked()` 未包裹 `blockSignals`，加载时触发 `toggled` 信号级联，双 True 脏数据导致两个复选框都被 `setEnabled(False)` 锁死
- 槽函数从 `setEnabled()` 禁用模式改为 `setChecked(False)` 自动互切模式（RadioButton 式），加 `if checked:` 守卫防止取消时误清对方
- `_on_announcement_toggled` 补即时写盘，与 `_on_announce_cache_toggled` 行为对称
- 配置加载用 `blockSignals` 包裹 + 双 True 仲裁（强制关闭 Web 缓存 + 同步禁用 URL 输入框 + 修正写盘）
- 新增 `tests/gui/test_settings_announce.py` 8 项 PyQt6 真实控件测试

### Web 端首页 UI 优化
- HomeView：解锁布局从 checkbox 改为按钮，移至右侧 170px 固定侧边栏，位于重置按钮正上方
- CARD_REGISTRY 新增 `zhName?` 字段，`hydrateLayout`/`addCard` 自动携带透传给子组件
- queryAdapter 卡片尺寸 6×8→8×10
- AdapterStatusQueryCard：卡片 minmax 70→110px，内边距加大，新增中文名称显示行（如 std_gov→国家标准），接收 `zhName` prop 作为标题
- 全局 Select/Dropdown 增强：发现 PrimeVue 4 用 `data-p-selected` 属性而非 `.p-highlight` 类名，修正选择器后品牌色高亮 + ✓ + 加粗生效
- `vue-tsc --noEmit` + HomeView 8 项测试通过

### 杂项修复
- LogBar 测试注入 mock router，消除 `Symbol(route location)` 警告
- CHANGELOG 补全 v0.55.34 条目，修复版本同步检查失败
- CI 根治：版本升版步骤增加 CHANGELOG.md 自动同步，以后不再出现版本不一致

## 2026-06-27（下午-晚间，49 commits）

### 代码质量门禁体系（GATE-04~12）
- **GATE-04**：46 条未登记 API 路由补全至压测方案文档，零遗漏
- **GATE-07**：死代码检测（Vulture + ts-prune）融入 CI 和 pre-commit
- **GATE-08**：依赖完整性检查（代码 import ↔ requirements-docker.txt 双向比对）
- **GATE-09**：CHANGELOG 版本一致性（CHANGELOG 最新版本 = __init__.py 版本），结合 CHANGELOG 历史补全（v0.5.27→v0.37.x，88 个版本）
- **GATE-10**：UI 敏感字段保护（.vue 中 secret/password/token/key 等字段必须用 Password 组件或 type="password"），NotificationConfig 三字段修复
- **GATE-11**：禁止硬编码 admin 作为权限标识，统一 ADMIN_ROLE 常量 → 后续废除改为 SUPERUSER_USERNAME 用户名判断
- **GATE-12**：Vue 组件 defineOptions 强制声明（防止 Vite 生产构建删除组件名），29 个组件批量修复
- `ca01280` commit #28 与 #27 无文件重叠，NotificationConfig Layout 调查完成

### CI/CD 完善
- **Shell 规范**：pyinstaller job Windows 混合语法步骤逐步骤指定 shell（pwsh/bash），写入 CLAUDE.md 和 capabilities_registry.md
- **dist.zip**：build-and-push job 中将前端构建产物打包上传至 release asset
- **APScheduler**：升级 4.x → 因 PyPI yanked 回退至 3.10.4，记录技术债

### Docker 自动更新重构
- `update.sh` 从文件覆盖式更新改为 MoviePilot 全量替换模式（下载 GitHub tag 归档 → 清空 /app → 拷贝新版本 → 写重启标记 → exit 重启）
- `entrypoint.sh` 改用 `source` 执行更新脚本，新增重启标记检测，新增 GATE-07/08/09/10 检查步骤
- `set -e` + `compare_versions` 返回值误杀修复

### 数据库迁移修复
- **v23 last_accessed_at**：`DEFAULT CURRENT_TIMESTAMP` → `DEFAULT NULL`（SQLite 不支持非字面量默认值）
- **v23 announcement_match**：`_bulk_upsert_cache` 移除显式事务控制（`Database.execute()` 已逐条自动提交）
- **v25 兜底**：`CREATE TABLE IF NOT EXISTS announcement_match`（v5 迁移可能因版本跳号被跳过）
- `conn.rollback()` 用 try/except 包裹（SQLite 自动提交模式下无活跃事务时报错掩盖真实 INSERT 失败）

### 依赖修复
- `watchdog==6.0.0` 补全到 `requirements-docker.txt`（pilotstd/monitor/ 已引用但未声明）
- `pydantic>=2.0.0` 显式声明

### 类型修复（TypeScript）
- 11 个 TS 错误修复：App.vue useToast、AppLayout toggleTheme、WechatTrustIP InputNumber、dashboard-migration.ts visible×7、SettingsView setLocale、TaskManager v-model
- `StatusStats` 类型与后端返回格式同步（total/active/repealed→current/expired/pending/upcoming）

### CHANGELOG 补全
- v0.5.27→v0.37.x 共 88 个版本，从 Git 历史自动提取，按 Added/Fixed/Changed 分类
- `0f2b832` fix: CI测试修复—前端Store适配+迁移空表防护+Node24环境变量

## 2026-06-26

### 方案 B：项目可安装化修复 CI 隐式依赖
- CI 测试无 `pip install -e .`，依赖 `sys.path[0]` 隐式导入 `pilotstd`
- pyproject.toml 补全 `[build-system]` + `[tool.setuptools]` + `[tool.setuptools.package-data]`
- ci.yml test-backend job 新增 `pip install -e .`
- 包安装验证：`pip install -e .` 成功，`import pilotstd.query.engine` OK
- sdist 构建验证：`python -m build --sdist` 成功，i18n/*.json 正确打包

### Vulture 死代码检测与清理
- 扫描 `pilotstd/ docker/ tests/ scripts/ --min-confidence=80`：25 项命中
- 确定死代码 7 处已删除：facade.py×3（recursive/cached_at）、commands.py×2（--shallow）、scan.py×1、rules_page.py×1、test_docker_api.py×1
- 误报 19 项记录到 whitelist.py（FastAPI Depends / 上下文管理器 / pytest fixture / @patch 注入）
- pyproject.toml 排除 whitelist.py（ruff extend-exclude + mypy exclude）

### 观测规则修复
- test_observability.py stress_web.py 检查模式：`PROGRESS_TAG`（值 `[PROGRESS]`）→ 导入语句
- stress_web.py 本身无需修改——已正确使用 `from pilotstd.query.engine import PROGRESS_TAG`
- 修复后 `python tests/test_observability.py --check-all`：5/5 PASS

### 6 条 GATE 门禁
- GATE-01~06 全部 PASS（GATE-04 29 条路由未登记为 WARN 非阻塞）

### Ruff 格式化
- 24 个测试文件行宽 120 字符约束格式化，净减少 220 行

### 提交记录
- `270607b` feat: 项目可安装化+死代码清理+CI包安装+观测规则修复
- `ba5322f` style: Ruff格式化 — tests/ 目录行宽120字符约束
- 代码已推送 origin/main，CI 已自动触发

## 2026-06-23

### 压测方案迭代（v7.0 → v9.0）
- 第〇步解散：`_step0_check_preconditions` 拆分为 `_step1/2/3_precheck()`
- Docker API 端点数修正：56→38（实际扫描确认）
- 源目录文件数更新：≥100→2488（实际部署源目录）
- 报告格式扩展：verdict.json 增加 meta/summary，新增 report.md
- v8.0：删除已废弃的第〇步章节
- v9.0：测试代码模块化拆分（stress_runner/stress_cli/stress_docker）

### 代码修复（9 项）
- db.py：4 处 sqlite3.connect 加 text_factory=str，根除 Python 3.14 bytes 类型错误
- engine.py：query_batch_parsed 增加 cache.put()，修复 standard_info_cache 始终为空的 bug
- auth.py：refresh_static_token() 回写 .env
- SettingsView.vue：复制按钮 HTTP 降级（execCommand）
- TaskView.vue：std_name 改用 f.std_name 优先
- ci.yml：build-and-push checkout bump 后的 tag
- stress_driver.py：源目录暂停逻辑改为 CLI 完成后始终暂停 + --yes 模式适配
- docker-compose.yml：挂载 .env（未推送，避免触发无意义 CI）
- rotator.py：[轮转器] → [ROTATOR]

### 自动化规则固化
- .claude/instructions.md v1.1：6 条规则（同步性验证/根因分析/跨环节传递/信息不足/错误发现/前置检查）
- .env 文件创建：PILOTSTD_API_TOKEN 持久化 + stress_driver.py 加载 load_dotenv

### 已知遗留
- Docker 容器 .env 挂载待部署（compose 文件已修改未推送）
- CI 版本号不一致问题反复 6 次，本次修到根（checkout ref）

### 经验教训
- 跨环节传递必须显式标注 A→B 的传递方式，不能依赖隐式环境继承
- 多文件修改前必须列出前置检查清单
- 用户提供凭证后立即持久化，不能只当一次性参数传递
- 信息不足时禁止编造理由，先查证再说话

### 待修清单
1. Docker API 令牌刷新不写 .env — 已修代码，compose 挂载待推送
2. Web 复制按钮 HTTP 下静默失败 — 已修
3. CI 镜像标签与内部版本号不一致 — 已修

### 代码风格统一
- ruff format 全量格式化 146 个文件（+7629/-3400）
- 导入排序：标准库 → 第三方 → 本地模块，字母序
- 类型注解完善：`Optional[X]` 替换 `X = None`，添加 mypy `type: ignore` 标记
- 删除 4 个过期 stress 归档文件（`stress_01_pipeline_archived` 等）

### 自检流程修复
- E741：`l` 模糊变量名 → `line`（logs.py, system.py, stress_driver.py）
- F821：`table_helper_mixin.py` 缺失 `QTableWidget` 导入
- F823：`download_mixin.py` 中 `_` 变量与 i18n 函数冲突 → `_ignored`
- F841：4 处未使用赋值变量清理
- E402：测试文件 per-file-ignores 配置 + 源文件 `# noqa: E402` 标记
- mypy：`callable` → `Callable`，添加 `jose`/`apscheduler.*` 忽略

### 配置与规则
- 创建 `pyproject.toml`：ruff（E/F/W/I 规则 + E501 忽略 + E402 测试豁免）、mypy（排除 tests/ui + 第三方缺 stub 忽略）
- `.gitignore` 新增诊断文件排除（`mypy_errors.txt`、`project_*_tree.txt`）
- CLAUDE.md 重构为 markdown 格式，新增 5.8~5.14 七条技术铁律，更新编码前后检查清单

### 推送记录
- `84c6fa6`：pyproject.toml + gitignore（已推送）
- `42019d2`：self_review 通过 — ruff format + lint + mypy（已推送）
- `3d966a1`：代码风格统一 + 删除过期 stress 归档（已推送）

## 2026-06-25

### Web 仪表板 6 阶段改造
- **阶段 0**：vue-grid-layout 安装、Dashboard Store、类型定义、语言选择（zh-CN/zh-TW/en）
- **阶段 1**：4 个 Widget（StatsCard/AdapterStatusCard/RecentAnnounceCard/QuickActionsCard）、HomeView 拖拽化
- **阶段 2**：熔断配置迁至设置页、DashboardView 删除
- **阶段 3**：公告英文标签中文化、用户删除确认弹窗 + 角色权限 + 密码标示 + admin 禁用
- **阶段 4**：通知配置整合进设置页、钉钉渠道 UI、响应式网格卡片布局、NotificationsView 删除
- **阶段 5**：时效性配置整合进设置页、执行记录扩展（筛选/分页/详情）、OrganizeView 复选框 + 入队 API、ValidityConfigView 删除
- **补充**：后端钉钉适配器（dingtalk.py HMAC-SHA256 加签）+ Manager 注册

### 代码统计
- 34 个文件变更：+2255/-996（仪表板改造）
- 9 个文件变更：+496/-94（压测脚本 + 缓存工具）
- 合计 43 文件，新增 ~12 个组件/Store/类型文件，删除 3 个旧页面
- 能力登记簿：68 → 85 条

### 架构变化
- 独立页面 4→1：DashboardView、NotificationsView、ValidityConfigView 删除，功能全部整合进 SettingsView（12 Tab）
- 首页静态网格 → vue-grid-layout 可拖拽仪表板（6 个 Widget）
- 支持语言：zh-CN → zh-CN/zh-TW/en 三语言
- 通知渠道：wechat/telegram/feishu → +dingtalk（前端 UI + 后端适配器完整实现）

### 推送记录
- `0da0dea`：feat: Web 仪表板 6 阶段改造（已推送）
- `5b269ff`：fix: 压测脚本适配 + 缓存检查工具 + 进度条 + Scheduler（已推送）

## 2026-06-28

### 调查与修复
- **首页卡片拖拽/缩放**：确认 grid-layout-plus 通过 vite-plugin-css-injected-by-js 运行时注入 CSS，无需额外 CSS 导入，功能在代码层面完整
- **CI 前端构建流程**：确认 build-and-push job 包含 npm ci + npm run build 步骤，web/dist/ 由 COPY 指令正确纳入镜像
- **删除按钮不可见**：根因 SUPERUSER_USERNAME 硬编码为 'admin'，改为 `import.meta.env.VITE_SUPERUSER_NAME || 'SUPERUSER'`
- **OCR Tab 掩码**：检查确认 6 个敏感字段全部使用 `<input type="password">`，掩码样式一致

### Main.ts 全局注册
- 24 个 PrimeVue 组件统一注册到 main.ts（Accordion ~ ToggleSwitch），消除各文件重复 import
- 移除无效的 grid-layout-plus/dist/styles.css 导入（该包无独立 CSS 文件）

### 新建门禁
- **GATE-13**：`scripts/check_gate_13.py` — 禁止前端代码硬编码 admin 作为超级用户标识，5 类检测模式，注册到 pre-commit hooks

### CHANGELOG
- 补全 v0.37.22（defineOptions + GATE-12）和 v0.37.23（全局注册 + SUPERUSER 修复 + GATE-13）条目

### 推送记录
- `533e2a9`：refactor: main.ts 全局注册 24 个 PrimeVue 组件（已推送）
- `37fce71`：fix: SUPERUSER_USERNAME 环境变量化（已推送）
- `c0fc971`：feat: 新增 GATE-13 门禁（已推送）
- `f659bb9`：docs: CHANGELOG 补全 v0.37.22~v0.37.23（已推送）

## 2026-07-09

### 待确认列表导出统一
- **问题**：汇总弹窗"保存待确认CSV"从 `_parsed_results` 过滤后的子集读数据，Word 模板等 `next_action != "pending"` 的条被过滤掉，CSV 为空
- **方案**：新增统一的 `_export_pending_csv()` 直接从 `pending_lookup` 表读取全部待确认记录，不过滤 `is_valid_standard`，不依赖界面状态
- **发现**：`_show_pending_dialog`、`_save_pending_csv`、`_build_pending_table` 三个方法均无调用方，一并删除（净减 124 行）
- **涉及文件**：pending.py（删 3 死方法 + 新增统一函数）、summary.py（分支调用统一函数）、3 个 i18n JSON（新增 `msg_no_pending_items`）

### 控制台窗口隐藏
- **问题**：双击 PilotStd.exe 弹出黑色命令行窗口
- **根因**：PyInstaller spec 中 `console=True`
- **修改**：`desktop/PilotStd.spec:116` → `console=False`
- **兼容性确认**：`main.py` 和 `logger.py` 已有 `--noconsole` 模式的流处理

### 推送记录
- `83583e5`：feat: 待确认列表导出统一 + 控制台窗口隐藏（已推送）
- `4f281b9`：docs: CHANGELOG 补录待确认导出统一 + 控制台窗口隐藏（已推送）
