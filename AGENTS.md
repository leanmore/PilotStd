# AGENTS.md — PilotStd 项目执行规则

> **来源**：本文档由 Claude Code 全局工作协议（`~/.claude/CLAUDE.md`）迁移改写而来，迁移日期 2026-08-21。
> **改写原则**：保留与工具无关的工程纪律；删除 Claude Code 专属表述（Skill 调用、`.claude/` 路径），改写为工具无关的执行要求；"A4 会话知识补充条目"来自历史会话知识提炼（见文末）。
> **适用范围**：任何在本仓库内执行编码/文档/运维任务的 AI 代理。

---

## 一、基本原则（工具无关，原样保留）

### R-001：先搜再动
**指令**：改代码前先搜索已有实现、记忆、文档、git log。确认没有现成的才写新的。
**验证标准**：
- 新增函数/模块前，`grep` 全代码库无同功能实现
- git log 中无已被否决的同方案

### R-002：动哪改哪
**指令**：改动只限目标范围，不顺手改无关代码、不重构没坏的东西。发现遗留死代码→告知用户，不擅自删。
**验证标准**：
- `git diff` 仅包含目标文件的必要变更
- 无"顺手改进"相邻代码、注释、格式的行为

### R-003：删干净
**指令**：删除/重命名任何公共 API 前，grep 全代码库（含测试 `--deselect`、`collect_ignore`、归档文件）。修完用质量工具扫一遍确认零遗漏。
**验证标准**：
- 删除后全库 grep 无陈旧引用
- `ruff`/`vulture`/`ts-prune` 等质量工具零残留报错

### R-004：联动改
**指令**：改代码时同步检查关联文档（spec/plan）、记忆文件、进度日志是否需要更新。不留过期信息。
**验证标准**：
- 代码变更涉及架构/API/DB 时，对应文档同步提交
- 无"文档与代码不一致"的过期信息

### R-005：先验再说
**指令**：声称"完成/通过/没问题"前，必须跑过验证命令、看过输出、确认结果。不准凭记忆或推测。
**验证标准**：
- 交付前运行测试与门禁脚本并留存输出
- 实际运行验证（非仅静态检查）

### R-006：先析再答
**指令**：收到问题→查代码/文档/记忆→分析→结论。禁止用客套话填补思考时间，禁止绕弯子。
**验证标准**：
- 回答引用具体代码/文档位置作为依据
- 无空泛应付式回答

### R-007：文档写完即展示
**指令**：写完设计、计划等文档后，主动告知用户产出位置，方便阅读和审核。
**验证标准**：
- 交付报告列出所有产出文档的路径

### R-008：需求不明主动问
**指令**：需求不清楚时主动提问澄清，不脑补、不瞎编。能力达不到时明确告知并讨论替代方案。
**验证标准**：
- 交付前无未经确认的假设
- 超出能力范围的请求明确说明

### R-009：网络请求本地发起
**指令**：所有网络连接和查询从本地机器发起，严禁通过远端服务器操作。
**验证标准**：
- 网络操作均在本地进程执行

### R-010：Bash 路径用正斜杠
**指令**：传给 Bash 工具的 Windows 路径用 `D:/path` 格式（反斜杠被转义会导致路径找不到）。
**验证标准**：
- Bash 命令中 Windows 路径无反斜杠

---

## 二、语言规则

### R-011：语言与注释规范
**指令**：
- thinking/分析过程：全程中文，含代码讨论、方案权衡、自检自查
- 对话输出：中文（代码引用中的标识符、路径、命令除外）
- 代码标识符：变量名、函数名、类名使用英文
- 代码注释：所有代码加中文注释，说明"为什么"而非"是什么"
**验证标准**：
- 代码审查抽查：注释解释动机而非复述代码
- 新增代码无英文占位注释

---

## 三、代码纪律

### R-012：简洁至上
**指令**：最小代码解决问题，不加用户没要求的功能。不为单次使用创建抽象。不加"灵活性/可配置性"除非用户要求。不为不可能发生的场景加错误处理。写了 200 行发现 50 行就够了→重写。
**验证标准**：
- 代码审查：无过度设计、无冗余抽象
- 自问"高级工程师会觉得过度复杂吗？"→是就简化

### R-013：精准修改
**指令**：只动目标代码，不动无关代码。不顺手"改进"相邻代码、注释、格式。不重构没坏的东西。匹配已有代码风格。发现无关的遗留死代码→告知用户，不擅自删除。自己的改动产生孤儿代码→清理自己制造的未使用 import、变量、函数。
**验证标准**：
- 每一行改动都能追溯到用户的明确要求
- 自己的改动无新增死代码

### R-014：SQL 安全
**指令**：动态表名须为同文件全大写常量。SQLite 不支持表名参数化，f-string 构建 SQL 时若表名来自变量，该变量必须在同一文件内以全大写常量定义。禁止将用户输入或外部数据作为表名。
**验证标准**：
- 代码扫描：无外部数据直接拼入表名
- 动态表名全部为同文件全大写常量

### R-015：完成前清理
**指令**：声称"完成"前，搜索自身改动区域内的 TODO、FIXME、stub，一个都不准留。确认每条改动用户现在就能用，不存在隐藏前提条件。
**验证标准**：
- 改动区域内 grep 无 TODO/FIXME/stub
- 每条改动可立即使用

---

## 四、工作流程（6 步，不可跳步）

### R-016：分析确认先行
**指令**：理解问题→提问澄清→出 1-2 个方案→等用户批准。未产出设计文档（`docs/superpowers/specs/` 或等效文档）→ 禁止进入实现。
**验证标准**：
- 功能开发前存在 spec/plan 文档
- 用户已批准方案

### R-017：测试驱动
**指令**：逻辑、算法、数据处理类变更→必须先写失败测试。可跳过：GUI 布局调整、i18n、配置修改、文档编写。适用范围内未写测试→禁止写实现代码。
**验证标准**：
- 变更伴随对应测试用例（tests/ 目录）
- 测试先行（先红后绿）

### R-018：验证完成
**指令**：跑测试+跑应用，亲眼看到通过。用真实数据验证，至少 3 条采样，不全信第一个返回值。未验证→禁止声称完成、禁止提交。
**验证标准**：
- 全量测试通过（pytest）
- 真实数据采样 ≥3 条
- 验证记录文件（`.test_pass`）存在且有效

### R-019：代码审查
**指令**：自审，对照需求逐条检查。发现问题→回到分析步骤重新出方案，禁止就地修改后跳过分析。
**验证标准**：
- 交付报告含自审清单
- 发现问题已走完整重新分析流程

### R-020：分支收尾
**指令**：合并、PR、清理。推送后必须确认 CI 通过+Release 创建成功，两项全部确认后才可声称"完成"。
**验证标准**：
- CI 通过 + Release 创建成功记录

---

## 五、异常处理

### R-021：异常处理流程
**指令**：
- 审查发现问题→回到分析步骤重新出方案、等批准
- 架构级问题→和用户讨论是否另起独立任务
- 外部依赖阻塞→明确说明原因，取得用户同意
**验证标准**：
- 处理过程符合上述分流

### R-022：能力矩阵同步（涉及架构变更时）
**指令**：当本次修改涉及以下任一情形时，必须在 git commit 之前执行 `python scripts/generate_capabilities.py`，将重新生成的 `docs/governance/capabilities_registry.md` 纳入同一 commit：
- 新增、修改或删除 ADR（`docs/adr/*.md`）
- 修改架构相关代码（`pilotstd/core/`、`pilotstd/query/engine/`、`docker/api/`）
- 修改 `docs/` 目录下任何治理文档
- `docs/governance/capabilities_registry.md` 本身被删除或意外丢失
若未同步更新导致 CI 门禁 G-032 失败，视为提交不完整，需立即补跑上述命令并 amend commit。
**验证标准**：
- 涉及架构变更的 commit 包含 `capabilities_registry.md` 变更（如生成结果有差异）
- G-032 文档健康度门禁通过

---

### 读文档触发条件表（G-037 依赖）

> 迁移自原 CLAUDE.md（2026-08-21），G-037 门禁脚本据此表与 `docs/index.md` 做对齐检查。

| 触发条件 | 必须读取的文档 |
|----------|---------------|
| 任何代码修改任务 | `docs/governance/development-flow.md` |
| 涉及数据库/Schema | `docs/architecture.md` |
| 涉及公告解析/入库 | `docs/reference/announcement-pipeline.md` |
| 涉及公告来源判断 | `docs/reference/announcement-sources.md` |
| 开发或修改适配器 | `docs/reference/adapter-development.md` |
| 涉及前端 UI 组件 | `docs/reference/ui-components.md` |
| 涉及 CI/CD 或门禁 | `docs/ci-lessons.md` |
| 涉及门禁配置变更 | `docs/governance/gates.md` |
| 涉及架构决策 | `docs/adr/` |
| 涉及技术债 | `docs/technical-debt.md` |
| 编写新代码时 | `docs/reference/coding-standards.md` |
| 提交代码前 | `docs/reference/pre-commit-checklist.md` |
| 不确定该读什么 | `docs/index.md` |

---

## 六、A4 会话知识补充条目（历史对话提炼）

> 以下条目由历史 Claude Code 会话转录（154 个会话文件，2026-07-18 ~ 2026-08-17）提炼而来，体现开发者本人的协作偏好与隐性约束（原始批次数据已随迁移清理删除）。

### P-101：先调查后修复（两阶段分离）
**指令**：调查与修改严格分离。调查阶段只读（禁止修改任何文件，即使发现 Bug 也不能顺手修，先回滚）；调查结论由用户拍板后才进入修复阶段。用户反复强调"只调查，不修改任何代码"。
**验证标准**：调查指令执行前后 `git status` 无差异；交付物为结论 + 证据，无修复代码。

### P-102：实证优先（先验再说）
**指令**：所有结论必须基于真实数据采样（≥3 条）或实测结果，禁止以纯代码逻辑推导替代数据采样；"不再接受任何基于纯代码分析的健康度评分"。
**验证标准**：结论附采样数据/实测输出；无"预期返回"代替实测结果。

### P-103：证据链交付
**指令**：交付必须附文件路径+行号、git diff、测试实际输出；报告是"执行结果"不是"待办清单"；禁止模糊描述（"不要省略、不要概括"）；无法定位的标注"⚠️ 未找到代码证据"。
**验证标准**：交付物每项可回查；无仅描述无证据的条目。

### P-104：修改范围严格受限
**指令**：用户习惯用"做/不做"清单界定范围（"严格遵守任务边界，不要修改任何未列出的功能"）；"一次只执行一个问题，验收通过后再启动下一项"；"不得删除、隐藏或覆盖任何原有配置项"。
**验证标准**：diff 仅含目标范围；无"顺手改进"。

### P-105：门禁纪律（不可绕过）
**指令**：禁止 `# noqa`、`--add-noqa`、`@ts-expect-error`、`as any`、`continue-on-error`、`skipif` 作弊绕过门禁；拒绝用削空白行/微调行数凑行数；预存错误"遇到了就消灭"；未经批准不得修改门禁脚本。
**验证标准**：门禁全绿靠真实修复而非豁免；无新增静默跳过。

### P-106：提交纪律
**指令**：Conventional Commits；按逻辑分组分批提交、全部完成后统一推送；测试修复作为独立 commit（标注 [Test-Fix]）与业务分离；同一原子变更的遗漏用 `--amend` 合入（"严禁新建 commit"）；**未经用户同意不得提交推送**；push 前先拉取远程确认无冲突。
**验证标准**：commit message 规范、分批合理、无未经同意的推送。

### P-107：交付报告必须完整
**指令**：每次任务结束必须交付完整报告（文件清单/测试结果/门禁结果/自审）——"连个报告都不给"是用户的强烈不满点；报告声称的交付物必须附实际代码/diff/输出证据。
**验证标准**：交付报告四要素齐全且有证据支撑。

### P-108：技术债处理原则
**指令**："预存问题也要解决"（修复范围不限于本次改动）；技术债单独排工单、登记不不了了之；硬编码参数被指出后立即参数化（"冷却期参数化现在就改"，不留到下个迭代）。
**验证标准**：无"后续跟进"式遗留；技术债有登记记录。

### P-109：分阶段确认式协作
**指令**：先列改动文件清单确认后再输出完整代码；方案审核通过才执行（"来审核这个指令，如有问题停下来反应，没有问题就执行"）；每步完成报告结果再进行下一步；"先确认再动手，不要擅自推进修改"。
**验证标准**：每个阶段有用户确认节点。

### P-110：生产环境敬畏
**指令**：三环境模型（本地开发 / Docker 生产 / 混合验证），环境自适应识别为强制前置动作；生产环境禁止直接操作（"你禁止直接操作此环境"）；写操作执行前生成结构化"写操作预览"等用户确认；数据库操作安全护栏（绝对禁止 DROP/TRUNCATE/无 WHERE DELETE/--force）。
**验证标准**：无生产环境直接写操作；写操作有预览确认记录。

### P-111：零推断、零偷懒
**指令**："极其彻底搜索""必须读取的文件逐个读取，不要跳过"；"全部侦查不要偷懒"；禁止凭记忆/经验推断代码内容（"严禁假设，必须以代码与运行时事实为依据"）；数据不足主动探测而不是停下来等。
**验证标准**：结论均基于实际读取的文件；无"我猜/应该"表述。

### P-112：输出格式与沟通
**指令**：调查输出用 `[结论] → 📍 文件路径:行号 → 💡 优化建议(Px)` 格式；汇报可用非技术业务语言（"只答是/否+通俗解释，不贴代码"）；用户下结论、AI 提供代码事实（"输出结论时只能写'代码显示 X'，结论由用户判断"）。
**验证标准**：输出格式符合约定；无替用户下结论的表述。

### P-113：执行节奏
**指令**：小步快跑、即时收口（"你干活，为何特别习惯拖沓，留尾巴呢？"）；低风险方案默认执行、高风险分步确认；"≤30 秒开始执行"；时间盒约束（外部依赖无法 mock 时立即跳过并记录，不得阻塞超过 2 分钟）。
**验证标准**：无拖沓留尾巴；阻塞及时上报。

### P-114：复用优先
**指令**：不引入新依赖（能用已有 beautifulsoup4+lxml 就不装 parsel）；复用已有机制（"预抽取"不为集成而集成、已有 skipIf(_CI) 机制不重复造轮子）；先搜再动。
**验证标准**：新增依赖/机制有充分理由。

### P-115：认知修正与情绪事件教训
**指令**：以下事件曾引发用户强烈不满，必须避免：①定时任务调查只到 register_job_func 层就下结论（必须追溯到底层实现）；②在只读调查任务中擅自修改代码（"谁让你修了？"）；③不看正在运行的 CI 就断言修复有效；④grep 截断输出 + 重跑全量测试（输出必须完整落盘）；⑤未读代码就下审核结论（"只列实际读到的证据，不推断"）。
**验证标准**：交付流程不触发上述行为。

### P-116：提示词偏差核实教训（CI 全绿冲刺 2026-08-21）
**指令**：任务提示词中的数字、路径、文件名、预期结果一律视为**待核实假设**，执行前先跑实测命令取得权威数据，以实测为准并如实报告偏差。本教训由 CI 全绿冲刺的 5 处偏差沉淀：
- ① 错误数：提示词称 G-038 "63 条/31 文件"，实测 **58 条/29 文件**（openstd_download.py、base.py 已无错误）
- ② 路径：提示词称 site_config 在 `pilotstd/core/`，实测在 **`pilotstd/query/`**
- ③ 行数对象：提示词称 "run_batch_tests.py 102 行"，实测是 **run_batch() 函数 102 行**超 G-010 的 80 行函数阈值（文件实际 272 行）
- ④ 文件名：提示词称修复 repo-compliance.yml，该文件**不存在**（GUI 测试在 ci.yml，pytest-qt 已正确配置）
- ⑤ 预期值：提示词称 G-032 应降到 ≤3 条，实测 8 条（其中 legacy-manual 6 条是设计保留）
**验证标准**：执行涉及具体数字/路径/文件的指令前，先运行实测命令（mypy/门禁脚本/glob 定位），交付报告中列出"提示词 vs 实测"差异表。

**P-116 补充（迁移 checksum 纪律，2026-08-23 沉淀）**：当项目存在迁移 checksum 校验机制（`_migration_checksum.py` 对已执行版本做函数源码哈希比对）时，**禁止对已执行版本的迁移函数进行任何形式的源码变更**——合并、拆分、重排、添加装饰器均会使 `inspect.getsource` 返回值变化，导致生产库 `_schema_version` 校验失败、启动即抛 `DatabaseError`。压缩 `migrations.py` 的**唯一安全方式**是将内联函数**逐字符原样移动**到独立模块（保留装饰器与函数体不变），保持 `inspect.getsource` 返回值不变；移动后用 `norm_checksum` 与生产库存储值逐版本实测比对确认一致。
