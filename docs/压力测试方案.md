# PilotStd 全量压力测试方案 v7.0

> （v7.0）补充前置条件+预期成果+四项原则审查记录附录；以 capabilities_registry.md v2.0（54条）为能力基准

## 0. 能力-场景映射表

> 基于 capabilities_registry.md v2.0（51 active + 3 deprecated），将每条活跃能力映射到压测场景。

### 0.1 CLI 端场景覆盖

| 能力 | 对应场景 | 验证方式 |
|------|---------|---------|
| `query/engine.py` - 60s 进度心跳 | 1.2 query | 检查 stderr 中 `[PROGRESS]` 输出间隔=60s |
| `query/engine.py` - 8-worker 桶间并行线程池 | 1.2 query | `[BUCKET]` 日志中桶数=worker数、并发耗时 < 串行 |
| `query/engine.py` - `[PROGRESS]`/`[BUCKET]`/`[FUNNEL]` 等 10 项观测日志 | 1.2 query, 4 汇总 | 日志解析成功率 > 95% |
| `query/rotator.py` - `[ROTATOR]`/`[COOLDOWN]` 日志 | 1.2 query | 冷却日志存在、站点里程碑输出 |
| `query/cache.py` - CacheRepository 双层缓存 | 1.2 query | `[CACHE]` hit/miss/rate% 在查询结束后统计 |
| `query/daily_quota.py` - DailyQuotaTracker | 1.2 query | 配额记录写入、锁竞争监控 |
| `task/queue.py` - 任务队列异步执行 | 1.8 task | `task --format json` 返回非空 |
| `scan/watcher.py` - 文件系统监控 | 1.1 scan | 扫描文件数 > 0 |
| `download/engine.py` - 下载线程池 | 1.3 download | 下载成功数 >= 0（允许采标跳过） |
| `core/file_index.py` - 文件索引延迟校验 | 1.5 organize | file_index 写入后记录数 = 扫描文件数 |
| `core/logger.py` - LoggerManager | 全局 | 日志轮转不丢失、I/O 不阻塞 |
| `core/updater.py` - 软件自更新 | 第〇步 | **已知限制**：压测期间禁用自更新，通过环境检查确认未运行 |
| `announcement/base.py` - 公告附件下载+详情并行 | 1.7 announce | OCR 处理完成、公告条目数 > 0 |
| `announcement/monitor.py` - 阶段耗时追踪 | 1.7 announce | `fetch_list_ms`/`fetch_detail_total_ms` 记录 |
| `announcement/engine.py` - AnnounceEngine | 1.7 announce | gb/hb/db 三类型均返回结果 |
| `announcement/ocr.py` - OCR 取消信号 | 1.7 announce | OCR 超时/取消时无泄漏 |
| `manager/scheduled_service.py` - 定时任务编排 | 1.9 recheck | **已知限制**：CLI 冷启时不触发定时任务，仅手动调用 |
| `organizer/expire_handler.py` - 过期处理 | 1.6 expire | 过期文件移至目标目录 |

### 0.2 WinUI 端场景覆盖

| 能力 | 对应场景 | 验证方式 |
|------|---------|---------|
| `ui/main_window.py` - 跨线程暂停信号 | 3 WinUI | 暂停/继续按钮响应 |
| `ui/main_window.py` - 软件更新下载线程 | 3 WinUI | 压测期间禁用 |
| `ui/main_window.py` - SIGTERM/atexit 自动保存 | 3 WinUI | 退出后状态文件存在 |
| `ui/workers.py` - 日志缓冲 QTimer | 3 WinUI | 日志刷新无卡顿 |
| `ui/workers.py` - AutoWorker | 3 WinUI | 四阶段完成，进度条到100% |
| `ui/pending_query_dialog.py` - 冷却倒计时刷新 | 3 WinUI | 冷却状态正确显示 |
| `ui/controllers/auto_run_mixin.py` - _ThrottledProgress | 3 WinUI | 进度条无跳变、节流间隔 500ms |

### 0.3 Web/Docker 端场景覆盖

| 能力 | 对应场景 | 验证方式 |
|------|---------|---------|
| `docker/scheduler.py` - APScheduler + 心跳 | 2 Docker | 调度器启动、`scheduler_lock` 心跳 |
| `docker/scheduler.py` - 公告任务独立线程 | 2 Docker | `auto_announce` 完成后缓存有数据 |
| `docker/app.py` - FastAPI 生命周期 | 2 Docker | startup/shutdown 钩子执行 |
| `docker/auth.py` - JWT + API Key | 2 Docker | AUTH-01/AUTH-02 认证验证 |
| `docker/api/announce.py` - 公告结果内存缓存 | 2 Docker | `/api/announce/results` 返回缓存数据 |
| `manager/facade.py` - Web 公告缓存回退 | 2 Docker | BIZ-12 缓存命中验证 |
| `manager/pending_service.py` - 离线双表回退 | 2 Docker | 离线查询降级验证 |
| `core/file_index.py` - 文件索引缓存字段恢复 | 2 Docker | 缓存恢复后字段完整性 |

---

## 1. 执行流程

```
stress_driver.py（总入口）
  ├── 启动前：rotator_state 清理（冷却状态重置）
  ├── 第〇步：环境自检 + 凭证预检 + 版本一致性校验 + 复位源目录 + 清五表 + stress_selfcheck 自检
  ├── 第一步：CLI 冷启（1.1 scan → 1.2 query → 1.3 download → 1.4 normalize
  │            → 1.5 organize → 1.6 expire → 1.7 announce → 1.7.1 cache_lookup → 1.8 task → 1.9 recheck）
  │     └── --stop-after=cli 可强制退出；否则 --yes 模式且 org_moved=0 自动继续
  ├── 【人工/自动】复位源目录（--yes 模式自动执行 _step0_reset_source）
  ├── 第二步：Docker Web API 验证（先于 WinUI 执行，填充 announcement_cache 供缓存命中验证）
  ├── 第三步：WinUI 热启交叉对比（甲轮本地回归 + 乙轮 Web 缓存验证）
  └── 第四步：汇总判定（流程状态 + 逐桶指标 + 适配器统计 + 凭证状态）
```

> 步骤顺序 CLI→Docker→WinUI。Docker 先行填充 announcement_cache，供 WinUI 乙轮验证缓存命中。

### 用法

```bash
# 完整压测
python tests/stress_driver.py --source D:\标准 --output E:\标准 --config tests/test_config.json --yes

# 只执行第一步（CLI 冷启），完成后退出
python tests/stress_driver.py --source D:\标准 --output E:\标准 --config tests/test_config.json --yes --stop-after=cli

# 跳过 CLI，从已有 step1.json 继续
python tests/stress_driver.py --source D:\标准 --output E:\标准 --config tests/test_config.json --yes --skip-cli --step1 <step1.json路径> --keep-db
```

### 核心参数

| 参数 | 作用 |
|------|------|
| `--source` / `--output` | 源目录和输出目录 |
| `--config` | JSON 配置文件路径（含 Docker/OCR/`web_api.url`） |
| `--yes` | 无人值守模式，跳过所有交互确认 |
| `--keep-db` | 跳过清 DB |
| `--skip-cli` / `--skip-winui` / `--skip-docker` | 跳过对应步骤 |
| `--stop-after cli` | CLI 后强制退出 |
| `--winui-only` | 仅执行 WinUI，需 `--step1` |
| `--step1` | step1.json 路径 |
| `--result-dir` | 统一输出目录 |
| `--timeout-query` | 查询超时（默认 3600s） |
| `--timeout-auto` | WinUI 超时（默认 1800s） |
| `--db-path` | 数据库路径 |

---

## 2. CLI 步骤失败处理决策表

| 步骤 | 失败条件 | 处理方式 | 原因 |
|------|---------|---------|------|
| 1.1 scan | 扫描文件数=0 或 rc≠0 | **终止** | 无有效数据 |
| 1.2 query | 成功率<10% 且 成功数<50 或 rc≠0 | **终止** | 数据不足 |
| 1.3 download | 任意下载失败 | 继续 | 不影响验证 |
| 1.4 normalize | rc≠0 | **终止** | 无法验证去重 |
| 1.5 organize | rc≠0 | **终止** | file_index 缺失 |
| 1.6 expire | rc≠0 | 继续 | 不影响核心 |
| 1.7 announce | rc≠0 | 继续 | 不影响核心 |
| 1.8 task | rc≠0 | 继续 | 不影响核心 |
| 1.9 recheck | 1.1~1.8 任一失败 | **跳过** | 数据不完整 |

---

## 3. 流程状态定义

| 状态 | 含义 |
|------|------|
| **FULL_PASS** | 1.1~1.9 全部通过 |
| **TERMINATED_EARLY ({step})** | scan/query/normalize/organize 失败 |
| **PARTIAL_FAIL** | 仅非关键步骤失败 |
| **SKIPPED** | 用户 --skip-* 跳过 |

---

## 4. 各步骤详述（含前置条件与预期成果）

### 第〇步：环境自检 + 清理 + 版本校验 + 凭证预检

**前置条件**：
- 环境状态：源目录存在且含 ≥100 个标准文件（PDF/DOC）；输出目录存在且可写；外网可达
- 依赖服务：`updater.py` 自更新已禁用（压测环境不触发）；`scheduled_service.py` 定时任务已暂停（仅压测脚本主动调用）
- Docker：`PILOTSTD_BASE_URL` 可达，`/api/health` 返回 200
- 凭证：`test_config.json` 含有效的 `docker.username`/`docker.password`

| 子步骤 | 内容 | 预期成果 | 失败处理 |
|--------|------|---------|---------|
| rotator_state 清理 | `DELETE FROM rotator_state` | 冷却状态归零，`rowcount ≥ 0` | WARN |
| 环境自检 | Python≥3.12, 源目录>0文件, 外网可达, Docker可达 | 所有检查项 PASS | 退出 |
| 凭证预检 | `POST /api/login` → 200 + `pilotstd_token` cookie | 登录成功，P95 < 3s | 退出 |
| 版本一致性 | `check_version_consistency()` 比较客户端/远端 `__version__` | 版本一致 | 退出（可 `SKIP_VERSION_CHECK=1` 跳过） |
| 复位源目录 | `_step0_reset_source()` | 无孤儿文件残留 | WARN |
| 清五表 | 清空 standard_info_cache/announcement_cache/pending_lookup/file_index/rotator_state | 5 表 rowcount=0 | — |
| selfcheck | 55 项纯逻辑自检 | **55/55 PASS**，耗时 < 10s | 退出 |

**预期成果汇总**：第〇步全部通过，耗时 < 30s。**错误率=0%**。

---

### 第一步：CLI 冷启

#### 1.1 scan

**前置条件**：
- 环境状态：源目录含 ≥100 个标准文件（pdf/doc/docx/xls/xlsx）；`file_index` 表已清空
- 依赖服务：`scan/watcher.py` 文件监控线程未启动（CLI 不走 watchdog）
- 配置文件：`storage.root_dir` 指向输出目录

| 指标 | 预期值 |
|------|--------|
| 扫描文件数 | ≥100（与源目录文件数一致） |
| rc | 0 |
| 响应时间 | P95 < 30s（每文件 < 300ms） |
| 错误率 | 0%（允许 ≤1% 无法解析，记录为 unrecognized） |

#### 1.2 query

**前置条件**：
- 环境状态：1.1 scan 成功，`step1_scan_nums.txt` 非空；外网可达 njbz365/std_gov/hbba/csres
- 依赖服务：`query/engine.py` 60s 心跳启用；`query/daily_quota.py` 配额追踪启用；各站点冷却状态已清
- 配置：`query.use_cache=true`，`query.use_announcement_cache=false`（CLI 冷启不走 Web 缓存）

| 指标 | 预期值 |
|------|--------|
| `[PROGRESS]` 心跳间隔 | **60s ± 5s**，无缺失 |
| 查询成功率 | download+expire+pending ≥ 10% × scan_count 或 ≥50 |
| `[BUCKET]` 桶数 | ≥1，并发耗时 < 串行预估 |
| `[CACHE]` 命中率 | > 0%（若有预置缓存）或 = 0%（冷启无缓存） |
| `[COOLDOWN]` 冷却日志 | 至少 1 条（各站点配额消耗后） |
| 配额记录 | daily_quota 表各站点当日计数 > 0 |
| 响应时间 | P95 < 3600s（全量查询） |
| 错误率 | 网络超时/连接重置类 < 5% |
| rc | 0 |

#### 1.3 download

**前置条件**：
- 环境状态：1.2 query 完成，`_download_list` 非空或有采标跳过
- 依赖服务：`download/engine.py` 下载线程池启用

| 指标 | 预期值 |
|------|--------|
| 下载成功数 | ≥0（允许全部采标跳过） |
| 响应时间 | P95 < 3600s |
| 错误率 | 单文件失败不阻断整体 |
| rc | 0（允许下载失败后 continue） |

#### 1.4 normalize

**前置条件**：
- 环境状态：1.2 query 结果中有 exact 匹配的标准

| 指标 | 预期值 |
|------|--------|
| 规范化成功数 | ≥ exact 匹配数 |
| rc | 0 |
| 响应时间 | P95 < 30s |

#### 1.5 organize

**前置条件**：
- 环境状态：1.4 normalize 成功

| 指标 | 预期值 |
|------|--------|
| 归档文件数 | ≥0（org_moved） |
| file_index 记录数 | = 扫描文件数（写入后验证） |
| rc | 0 |
| 响应时间 | P95 < 60s |

#### 1.6 expire

**前置条件**：
- 环境状态：1.5 organize 完成，存在 status="废止" 的标准

| 指标 | 预期值 |
|------|--------|
| 过期处理数 | ≥0 |
| rc | 0（失败不阻断） |
| 响应时间 | P95 < 30s |

#### 1.7 announce

**前置条件**：
- 环境状态：外网可达 std.samr.gov.cn；OCR 凭证配置有效（可选）；`announcement_cache` 表已清空（冷启）
- 依赖服务：`announcement/engine.py` 适配器调度；`announcement/base.py` 详情并行线程池（`_MAX_DETAIL_WORKERS`）；`announcement/ocr.py` OCR provider（可选，无凭证则跳过 OCR）
- 公告同步：**空窗期启动**——无预置缓存，首次抓取即为冷数据填充

| 指标 | 预期值 |
|------|--------|
| 公告抓取总数（total_ann） | > 0（gb/hb/db 各 pageSize=10） |
| 匹配数（matched） | ≥ 1（与 file_index 交叉比对） |
| `announcement_cache` 写入 | DISTINCT standard_number 数 = 1.9 checked |
| OCR 处理 | 若有凭证：OCR 调用成功；无凭证：降级跳过 |
| `monitor.py` 阶段耗时 | fetch_list_ms/fetch_detail_total_ms 均有记录 |
| rc | 0（允许单个适配器失败后 continue） |
| 响应时间 | P95 < 120s（含网络+OCR） |

#### 1.7.1 cache_lookup

**前置条件**：
- 环境状态：1.7 announce 成功（total_ann > 0）；`web_api_url` 已配置；API Token 有效
- 依赖服务：Docker 端 `/api/announce/lookup` 接口可达

| 指标 | 预期值 |
|------|--------|
| 查询次数 | 前 5 个标准号 |
| 命中率 | hits ≥ 0（取决于 announcement_cache 数据） |
| 响应时间 | P95 < 5s（5 次 HTTP GET） |

#### 1.8 task

**前置条件**：
- 环境状态：前述步骤已产生任务记录

| 指标 | 预期值 |
|------|--------|
| 任务队列 | 返回 json 数组 |
| rc | 0 |
| 响应时间 | P95 < 5s |

#### 1.9 recheck

**前置条件**：
- 环境状态：1.1~1.8 全部 rc=0；`announcement_cache` 表有数据

| 指标 | 预期值 |
|------|--------|
| checked | ≥ 1（DISTINCT standard_number） |
| updated | = 1.7 的 total_ann |
| rc | 0 |
| 响应时间 | P95 < 1s |

---

### 第二步：Docker Web API 验证

**前置条件**（整体）：
- 环境状态：第一步 CLI 冷启成功完成；Docker 容器运行中（`/api/health` → 200）；`announcement_cache` 已由第一步填充
- 依赖服务：`docker/scheduler.py` APScheduler 启动（`scheduler_lock` heartbeat 更新中）；`docker/auth.py` JWT+API Key 验证启用；`docker/app.py` FastAPI lifespan 钩子已执行
- 认证：API Token 已设置（`PILOTSTD_API_TOKEN` 环境变量），Bearer 鉴权可用
- 管理员账号：`admin` / 对应密码可登录

| 测试分组 | 项数 | 关键指标 | 预期成果 |
|---------|:----:|---------|---------|
| 认证（AUTH） | 4 | AUTH-01 有效令牌→200, AUTH-02 无效令牌→401 | 错误率 0% |
| 业务 API | 20+ | 查询/扫描/下载/归档/待确认/统计 均返回 200/404 | P95 < 2s/接口 |
| 配置与公告 | 8 | 设置读写一致；公告抓取后 `/api/announce/results` 有数据 | 配置一致性 100% |
| 文件操作 | 8 | 上传/列表/读取路径可访问 | P95 < 5s |
| 端到端管线 | 8 | 扫描→查询→规范化→归档 全链路 | 管线完成，文件进入输出目录 |
| 权限 | 4 | 非 admin 用户改设置→403 | 错误率 0% |
| **合计** | **56** | | **错误率 < 1%** |

**各分组预期成果**：

| 分组 | 前置条件 | 预期成果（量化） |
|------|---------|----------------|
| AUTH | Token 有效/无效各一 | AUTH-01: 200 + `pilotstd_token` cookie; AUTH-02: 401; 响应 P95 < 500ms |
| 业务 API | 标准号已存在于缓存 | 各接口 P95 < 2s; 业务逻辑正确（状态码 + 响应体字段完整） |
| 配置与公告 | 管理员已登录 | 配置读写 100% 一致; 公告抓取 P95 < 60s; 结果缓存命中 |
| 文件操作 | inbox 有测试文件 | 上传成功（201）; 列表可访问（200）; P95 < 5s |
| 端到端 | inbox 有标准文件 | 扫描→查询→规范化→归档 全链路; 归档后文件进入 `/standards` |
| 权限 | 普通用户 Token | 非 admin 修改设置→403; 读取设置→200 |

**整体预期成果汇总**（Docker 端）：
- 56 项检查中 **≥54 项 PASS**
- 认证断言准确率 100%
- 业务 API 响应 P95 < 2s
- 端到端管线完成率 100%（文件进入输出目录）

---

### 第三步：WinUI 热启交叉对比

**前置条件**（整体）：
- 环境状态：第一步 success（DB 缓存+file_index 已就绪）；第二步 success（announcement_cache 已由 Docker 填充）
- 依赖服务：`ui/main_window.py` 跨线程暂停信号可用；`ui/workers.py` AutoWorker 线程池正常；`ui/controllers/auto_run_mixin.py` _ThrottledProgress 500ms 节流生效
- WinUI 状态：GUI 已启动，用户已登录，暂停事件未设置
- 配置：`data/config.json` 由 stress_driver 写入 `use_announcement_cache` 字段

| 轮次 | 配置 | 交叉对比项 | 预期成果 |
|:----:|------|-----------|---------|
| 甲轮 | `use_announcement_cache=false` | scan_count, query_download, query_exact | 与 CLI step1 偏差 ≤10%（PILOTSTD_TOLERANCE=0.1） |
| 乙轮 | `use_announcement_cache=true` | 同上 + 来源标注验证 | 偏差 ≤10%；缓存命中项的 `source` 字段 = `"announcement_cache"` |

**各对比项预期成果**：

| 对比项 | 容差 | 预期 | 失败处理 |
|--------|:----:|------|---------|
| scan_count | ±10% | CLI=WinUI（同源目录） | 超过容差→FAIL |
| query_download | ±10% | CLI≈WinUI（热启复用缓存） | 超过容差→FAIL |
| query_exact | ±10% | CLI≈WinUI | 超过容差→FAIL |

**整体预期成果汇总**（WinUI 端）：
- 甲轮 verdict = PASS
- 乙轮 verdict = PASS（若 web_api_url 已配置）
- 进度条无跳变（_ThrottledProgress 500ms 节流生效）
- AutoWorker 四阶段全部完成，信号不丢失
- 异常时进度条变红 + 状态栏错误信息（v6.0 新增功能验证）

---

### 第四步：汇总判定

| 指标 | 预期值 |
|------|--------|
| 流程状态 | **FULL_PASS** |
| 凭证预检 | PASS |
| 适配器统计报告 | `get_adapter_report()` 返回非空、各站点 total_queries > 0 |
| 日志解析失败行数 | < 总日志行数的 5% |

---

## 5. 配置文件

```json
{
  "docker": { "url": "...", "username": "...", "password": "..." },
  "ocr": {
    "baidu_api_key": "...", "baidu_secret_key": "...",
    "tencent_secret_id": "...", "tencent_secret_key": "...",
    "aliyun_access_key_id": "...", "aliyun_access_key_secret": "..."
  },
  "web_api": { "url": "http://192.168.1.18:9028" }
}
```

凭证加载优先级：`--config JSON` > 环境变量 > `ConfigManager`。OCR 字段先取 JSON，空值回退 ConfigManager。`SKIP_VERSION_CHECK=1` 可跳过版本校验。

---

## 6. 相关文件

| 文件 | 用途 |
|------|------|
| `tests/stress_driver.py` | 总入口（~2100 行） |
| `tests/stress_web.py` | Docker API 验证（56 项） |
| `tests/stress_winui.py` | WinUI 交叉对比（甲/乙两轮） |
| `tests/stress_selfcheck.py` | 纯逻辑自检（55 项） |
| `tests/_stress_utils.py` | 公共工具：日志/判定/凭证/版本校验 |
| `docs/governance/capabilities_registry.md` | 能力登记簿 v2.0（54 条） |

---

## 7. 已知限制

| 限制 | 影响场景 | 风险接受 |
|------|---------|---------|
| `core/updater.py` 自更新无法在压测中验证 | 第〇步 | 低——压测环境禁用自更新，通过环境自检确认 |
| `manager/scheduled_service.py` 定时任务 CLI 不触发 | 1.9 recheck | 低——仅 Docker 端验证定时调度，CLI 端手动调用 |
| 公告平台实时数据不可回放 | 1.7 announce | 中——依赖外部站点可用性；`pageSize=10` 限制减轻影响；失败不阻断 |
| OCR 服务依赖第三方凭证 | 1.7 announce | 低——无凭证时降级跳过 OCR，验证不依赖 OCR 结果 |
| WinUI 乙轮依赖 Docker 先行 | 3 WinUI 乙轮 | 低——`web_api_url` 未配置时自动跳过乙轮 |

---

## 附录 A：四项原则审查记录

> 审查日期：2026-06-23
> 基准文档：capabilities_registry.md v2.0（54 条）
> 审查范围：压测方案 v7.0 全部场景

### 审查标准

| 原则 | 检查要点 | 判定标准 |
|------|---------|---------|
| **真实性** | 并发数/间隔/顺序模拟真实用户行为 | 并发数 ≤ 实际使用场景上限（单机 ≤ 8 worker） |
| **可重复性** | 多次运行结果可复现，外部依赖可固化 | 有固化数据源（源目录文件固定），外部 API 失败不阻断 |
| **可观测性** | 关键指标可监控和记录 | 每场景有日志输出/断言采集点，判定结果可追溯 |
| **非破坏性** | 使用隔离环境，有数据回滚/清理 | 使用独立输出目录，清五表可恢复，不操作生产数据 |

### 逐场景审查

| 场景 | 真实性 | 可重复性 | 可观测性 | 非破坏性 | 修改次数 | 备注 |
|------|:--:|:--:|:--:|:--:|:--:|------|
| 第〇步-环境自检 | ✅ | ✅ | ✅ | ✅ | 0 | — |
| 第〇步-凭证预检 | ✅ | ✅ | ✅ | ✅ | 0 | — |
| 第〇步-版本校验 | ✅ | ✅ | ✅ | ✅ | 0 | SKIP_VERSION_CHECK=1 可跳过 |
| 第〇步-复位源目录 | ✅ | ✅ | ✅ | ✅ | 0 | — |
| 第〇步-清五表 | ✅ | ✅ | ✅ | ✅ | 0 | — |
| 第〇步-selfcheck | ✅ | ✅ | ✅ | ✅ | 0 | 55 项纯逻辑，零网络依赖 |
| 1.1 scan | ✅ | ✅ | ✅ | ✅ | 0 | 源目录文件固定 |
| 1.2 query | ✅ | ⚠️ | ✅ | ✅ | 1 | 依赖外部站点可用性→已知限制；60s 心跳可观测 |
| 1.3 download | ✅ | ⚠️ | ✅ | ✅ | 1 | 同 query，外部站点依赖 |
| 1.4 normalize | ✅ | ✅ | ✅ | ✅ | 0 | 纯本地处理 |
| 1.5 organize | ✅ | ✅ | ✅ | ✅ | 0 | 纯本地处理 |
| 1.6 expire | ✅ | ✅ | ✅ | ✅ | 0 | 纯本地处理 |
| 1.7 announce | ✅ | ⚠️ | ✅ | ✅ | 1 | 外部站点+OCR 依赖→已知限制；失败不阻断 |
| 1.7.1 cache_lookup | ✅ | ✅ | ✅ | ✅ | 0 | 依赖 Docker+API Token，可跳过 |
| 1.8 task | ✅ | ✅ | ✅ | ✅ | 0 | 纯本地查询 |
| 1.9 recheck | ✅ | ✅ | ✅ | ✅ | 0 | 纯本地 SQL |
| Docker-AUTH | ✅ | ✅ | ✅ | ✅ | 0 | Token 可固化 |
| Docker-业务API | ✅ | ✅ | ✅ | ✅ | 0 | 依赖第一步数据 |
| Docker-配置公告 | ✅ | ⚠️ | ✅ | ✅ | 1 | 公告抓取依赖外部站点 |
| Docker-文件操作 | ✅ | ✅ | ✅ | ✅ | 0 | 测试文件可控 |
| Docker-端到端 | ✅ | ⚠️ | ✅ | ✅ | 1 | 管线依赖外部查询 |
| Docker-权限 | ✅ | ✅ | ✅ | ✅ | 0 | Token 可控 |
| WinUI-甲轮 | ✅ | ✅ | ✅ | ✅ | 0 | 本地回归，容差 10% |
| WinUI-乙轮 | ✅ | ⚠️ | ✅ | ✅ | 1 | 依赖 Docker 先行→已知限制 |
| 第四步-汇总判定 | ✅ | ✅ | ✅ | ✅ | 0 | — |

**审查结论**：全部 26 个场景通过四项原则审查。6 个场景有外部依赖（标注 ⚠️），已在"已知限制"中记录风险接受。

### 审查统计

| 指标 | 值 |
|------|-----|
| 场景总数 | 26 |
| 一次通过 | 20 |
| 迭代修改后通过 | 6 |
| 未通过 | 0 |
| 已知限制 | 5 条 |

---

## 附录 B：版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v7.0 | 2026-06-23 | 补充能力-场景映射表；全场景添加前置条件与量化预期成果；四项原则审查记录附录；已知限制记录 |
| v6.0 | 2026-06-23 | （阶段一产出）能力登记簿 v2.0 基准对齐 |
| v5.0 | 2026-06-23 | （阶段一产出）以 capabilities_registry.md 为基准更新 |
| v3.0 | 2026-06-23 | 以 f4ab5ec 代码现状重写：步骤顺序修正、参数补全、新功能补充 |
| v2.0 | 2026-06-20 | 原始版本 |
