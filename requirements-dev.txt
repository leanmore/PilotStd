# ==================================
# PilotStd 开发依赖清单
# 安装：pip install -r requirements-dev.txt
# ==================================

# --- 浏览器自动化与网络逆向分析 ---
playwright>=1.49.0

# --- 代码质量与测试 ---
ruff>=0.5.0
pytest>=8.3.0
pytest-qt>=4.2.0
pytest-cov>=6.0
responses>=0.25
respx>=0.23.1
pytest-httpserver>=1.1

# --- 测试工具（CI 并行与超时控制） ---
# 版本上限 <4 / <3：防止大版本升级引入破坏性变更（迁移记忆：pytest 9.x 已踩过 conftest 行为变更的坑）
pytest-xdist>=3.6,<4
pytest-timeout>=2.3,<3

# --- 打包构建 ---
pyinstaller>=6.0

# --- Locust 压测框架 ---
locust>=2.31

# --- 前端开发 ---
# Node.js >= 22.0.0
# npm >= 10.0.0

# 前端依赖变更（2026-06-26）
# - @primevue/themes@4.x → @primeuix/themes@1.x
# - vue-i18n@9.x → vue-i18n@11.x
