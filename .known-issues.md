# 已知问题（Known Issues）

本文件记录当前已知但尚未修复的问题，按类型分类。

## SECURITY

- **SECURITY: /api/logs decorator order inverted** (`@require_role` outside `@router.get`) — auth bypass risk, pending dedicated security review.
  - 位置：`docker/api/logs.py:31-33`
  - 发现日期：2026-08-15
  - 说明：`@require_role("admin")` 装饰器写在 `@router.get` 外层，Python 装饰器自下而上应用，导致 `router.get` 注册的是未包裹的原函数，`require_role` 的 wrapper 未生效，`/api/logs` 实际无需 admin 角色即可访问。
