# Known Security Issues

## SEC-001: Systemic @require_role Decorator Order Bug (Auth Bypass) — ✅ RESOLVED

**Severity**: High
**Discovered**: 2026-08-15
**Resolved**: 2026-08-15
**Root Cause**: `@require_role("admin")` applied *outside* `@router.get/post` → FastAPI registers the unwrapped function → admin auth enforcement never executed.

---

### Resolution Summary

| Category | Count | Details |
| :--- | :--- | :--- |
| Restored require_role (admin-only) | 46 | logs 3 + settings 8 + api_keys 5 + cache 4 + tasks 7 + scan 2 + monitor 6 + system 3 + **users 4 + wechat_ip 4** |
| Removed require_role (design correction) | 10 | notification 9 (user-level module) + system get_version 1 (public endpoint) |
| Natively correct | 1 | admin_db.py |
| **Total handled** | **57** | — |

### 修复决策历史（审计轨迹）

1. **P0**（settings/api_keys/cache）：恢复 require_role + 清理 AUTH_WHITELIST `/api/settings` GET 矛盾（admin only 合理）
2. **P1**（tasks/scan/monitor）：恢复 require_role（admin 功能）
3. **P1 设计修正**（notification）：移除 require_role——通知是用户级功能，注释反复强调"按用户隔离"，前端 notification-logs 路由无 permission（全角色可见），AuthMiddleware 认证 + user_id 隔离已足够
4. **P2**（system）：update/health/resources 恢复 require_role；get_version 降级公开（移除 require_role + **移除 container_id 防容器逃逸信息泄露**）
5. **遗漏修复**（users/wechat_ip）：AST 检查脚本 check_decorator_order.py 首次运行发现手工 grep 被截断而遗漏的 8 处（users 4 + wechat_ip 4），已恢复 require_role

### Post-resolution cleanup

| Action | Details |
| :--- | :--- |
| api_keys.py deleted | 5 个接口均为 410 Gone（功能迁移至 settings.py），已删除文件 + app.py 路由注册 + vulture 白名单条目 |

### 防回潮机制

- `scripts/check_decorator_order.py`：AST 静态检查，扫描 `docker/api/` 下所有 `@require_role` 装饰器顺序，已接入 `.pre-commit-config.yaml`（local hook `check-decorator-order`）

---

## TECH-DEBT

- **（已解决）`docker/api/api_keys.py` 废弃文件**：已物理删除（SEC-001 收尾阶段），消除死代码。
